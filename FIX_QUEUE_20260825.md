# Fix queue

- Repair broken PNG rendering in the recent drops feed.
- Resolve the exact asset path/name for MP7 Lich.
- Fix admin Z-Coin GRANT and REVOKE actions so the admin UI submits a valid request and the balance actually updates.

Tracking commit for the current terminal branch fix pass.