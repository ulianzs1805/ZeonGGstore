"use client";

import Link from "next/link";
import Image from "next/image";
import { useCallback, useEffect, useState } from "react";
import ImageUploadField from "./ImageUploadField";
import AdminSupportPanel from "./AdminSupportPanel";
import ZCoinPanel from "./ZCoinPanel";
import RoleManagementPanel from "./RoleManagementPanel";
import DevConsolePanel from "./DevConsolePanel";
import UsersPanel from "./UsersPanel";
import ForceDropPanel from "./ForceDropPanel";
import DashboardPanel from "./DashboardPanel";
import SkinPricePanel from "./SkinPricePanel";

type Role = "ADMIN" | "DEV" | "NPN1_DEV" | "TESTER";
type Rarity = "Common" | "Uncommon" | "Rare" | "Epic" | "Legendary" | "Mythic" | "ARCANE" | "NAMELESS";
type DropDraft = { name: string; rarity: Rarity; image: string; price: number; probability: number };
type Section = "dashboard" | "create" | "cases" | "drops" | "support" | "users" | "roles" | "myAudit" | "audit" | "economy" | "transactions" | "zcoin" | "console" | "force" | "tools" | "tester" | "skinPrices";
type CatalogCase = { id: string; slug: string; name: string; image: string; price: number; isActive: boolean; probabilityMode: "MANUAL" | "DYNAMIC"; createdAt: string; createdById: string; drops: Array<{ id: string; name: string; rarity: string; price: number; probability: number; image: string }> };
const rarities: Rarity[] = ["Common", "Uncommon", "Rare", "Epic", "Legendary", "Mythic", "ARCANE", "NAMELESS"];
const newDrop = (): DropDraft => ({ name: "", rarity: "Rare", image: "", price: 100, probability: 0 });

export default function AdminPanel({ role, email, staffId }: { role: Role; email: string; staffId: string | null }) {
  const [step, setStep] = useState(1);
  const [name, setName] = useState("");
  const [caseImage, setCaseImage] = useState("");
  const [casePrice, setCasePrice] = useState(199);
  const [probabilityMode, setProbabilityMode] = useState<"MANUAL" | "DYNAMIC">("MANUAL");
  const [probabilityPreview, setProbabilityPreview] = useState<Array<{ name: string; rarity: string; price: number; baseWeight: number; calculatedProbability: number }>>([]);
  const [previewError, setPreviewError] = useState("");
  const [drops, setDrops] = useState<DropDraft[]>([newDrop()]);
  const [message, setMessage] = useState("");
  const [createdCase, setCreatedCase] = useState<{ slug: string; name: string; drops: number } | null>(null);
  const [submitting, setSubmitting] = useState(false);
  const [section, setSection] = useState<Section>("dashboard");
  const [cases, setCases] = useState<CatalogCase[]>([]);
  const [records, setRecords] = useState<unknown[]>([]);
  const [sectionLoading, setSectionLoading] = useState(false);
  const [sectionError, setSectionError] = useState("");
  const [mobileNavOpen, setMobileNavOpen] = useState(false);
  const totalProbability = drops.reduce((sum, drop) => sum + (Number.isFinite(drop.probability) ? drop.probability : 0), 0);
  const validDraft = Boolean(name.trim() && caseImage && drops.length > 0 && drops.every((drop) => drop.name.trim() && drop.image && Number.isInteger(drop.price) && drop.price > 0 && (role === "ADMIN" || drop.probability > 0)) && (role === "ADMIN" || Math.abs(totalProbability - 100) < 0.001));

  const updateDrop = (index: number, patch: Partial<DropDraft>) => setDrops((current) => current.map((drop, dropIndex) => dropIndex === index ? { ...drop, ...patch } : drop));
  const next = () => { setMessage(""); if (!name.trim() || !caseImage) { setMessage("Загрузите изображение и укажите название кейса."); return; } setStep(2); };
  const createCase = async () => {
    setSubmitting(true); setMessage("");
    try {
      const response = await fetch("/api/admin/cases", { method: "POST", cache: "no-store", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ name, image: caseImage, price: casePrice, probabilityMode, drops }) });
      const data = await response.json().catch(() => null);
      if (!response.ok) setMessage(data?.error || "Не удалось создать кейс.");
      else { setCreatedCase({ slug: data.case.slug, name: data.case.name, drops: data.case.drops.length }); setMessage("Кейс успешно создан и записан в audit log."); }
    } catch { setMessage("Ошибка сети. Проверьте подключение и попробуйте ещё раз."); }
    finally { setSubmitting(false); }
  };

  const endpointForSection = useCallback((target: Section) => target === "cases" || target === "drops" || target === "economy" || target === "tools" ? "/api/admin/cases" : target === "tester" ? "/api/tester/cases" : target === "audit" || target === "myAudit" ? "/api/admin/audit" : target === "transactions" ? "/api/admin/transactions" : null, []);
  const loadSection = useCallback(async (target = section, silent = false) => {
    const endpoint = endpointForSection(target); if (!endpoint) return;
    if (!silent) setSectionLoading(true); setSectionError("");
    try {
      const response = await fetch(endpoint, { cache: "no-store", headers: { "Cache-Control": "no-cache" } });
      const data = await response.json().catch(() => null);
      if (!response.ok) setSectionError(data?.error || "Не удалось загрузить раздел");
      else if (["cases", "drops", "economy", "tools", "tester"].includes(target)) setCases(data.cases ?? []);
      else setRecords(target === "audit" || target === "myAudit" ? data.logs ?? [] : data.transactions ?? []);
    } catch { setSectionError("Ошибка сети. Не удалось обновить данные."); }
    finally { if (!silent) setSectionLoading(false); }
  }, [endpointForSection, section]);

  useEffect(() => { if (!["dashboard", "create", "support", "zcoin", "roles", "console", "users", "force", "skinPrices"].includes(section)) void loadSection(section); }, [section, loadSection]);
  useEffect(() => { const refresh = () => { if (document.visibilityState === "visible") void loadSection(section, true); }; document.addEventListener("visibilitychange", refresh); window.addEventListener("focus", refresh); window.addEventListener("pageshow", refresh); return () => { document.removeEventListener("visibilitychange", refresh); window.removeEventListener("focus", refresh); window.removeEventListener("pageshow", refresh); }; }, [loadSection, section]);
  useEffect(() => { setMobileNavOpen(false); }, [section]);

  const sections: Array<{ id: Section; label: string; visible: boolean }> = [
    { id: "dashboard", label: "Обзор", visible: true }, { id: "create", label: "Создать кейс", visible: true }, { id: "cases", label: "Управление кейсами", visible: true }, { id: "drops", label: "Дропы", visible: true }, { id: "users", label: "Пользователи", visible: true }, { id: "roles", label: "Выдать роль", visible: role === "DEV" || role === "NPN1_DEV" }, { id: "support", label: "Поддержка", visible: true }, { id: "myAudit", label: "Мои действия", visible: true }, { id: "economy", label: "Экономика", visible: role !== "ADMIN" }, { id: "transactions", label: "Транзакции", visible: role !== "ADMIN" }, { id: "zcoin", label: "Z-Coin", visible: role !== "ADMIN" }, { id: "console", label: "Dev Console", visible: role !== "ADMIN" }, { id: "audit", label: "Audit Logs", visible: role !== "ADMIN" }, { id: "force", label: "Force Drop / Test Drop", visible: role === "NPN1_DEV" }, { id: "skinPrices", label: "Стоимость скинов", visible: role === "DEV" || role === "NPN1_DEV" }, { id: "tester", label: "Tester Tools", visible: role === "TESTER" || role === "DEV" || role === "NPN1_DEV" }, { id: "tools", label: "Developer Tools", visible: role !== "ADMIN" },
  ];
  const visibleSections = sections.filter((item) => item.visible); const activeLabel = visibleSections.find((item) => item.id === section)?.label ?? "Админка";
  const toggleCase = async (item: CatalogCase) => { const endpoint = section === "tester" ? "/api/tester/cases" : "/api/admin/cases"; const response = await fetch(endpoint, { method: "PATCH", cache: "no-store", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ caseId: item.id, isActive: !item.isActive }) }); const data = await response.json().catch(() => null); if (!response.ok) setSectionError(data?.error || "Не удалось изменить статус кейса"); else await loadSection(section, true); };
  const toggleProbabilityMode = async (item: CatalogCase) => { const nextMode = item.probabilityMode === "DYNAMIC" ? "MANUAL" : "DYNAMIC"; const response = await fetch("/api/admin/cases", { method: "PATCH", cache: "no-store", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ caseId: item.id, probabilityMode: nextMode }) }); const data = await response.json().catch(() => null); if (!response.ok) setSectionError(data?.error || "Не удалось изменить режим вероятностей"); else await loadSection(section, true); };

  const renderDataSection = () => {
    if (sectionLoading) return <p className="rounded-xl border border-white/10 p-5 text-sm text-slate-400">Загрузка...</p>;
    if (sectionError) return <div className="space-y-3 rounded-xl border border-red-400/30 bg-red-500/10 p-5 text-sm text-red-200"><p>{sectionError}</p><button type="button" onClick={() => void loadSection(section)} className="rounded-lg border border-red-300/30 px-3 py-2 text-xs font-bold">Повторить</button></div>;
    if (section === "cases" || section === "tester") return cases.length ? <div className="grid gap-4 md:grid-cols-2">{cases.map((item) => <article key={item.id} className="min-w-0 rounded-2xl border border-white/10 bg-black/15 p-4"><div className="relative h-32"><Image src={item.image} alt={item.name} fill className="object-contain" unoptimized /></div><div className="mt-3 flex flex-wrap items-start justify-between gap-3"><h3 className="break-words font-black">{item.name}</h3><span className={`shrink-0 rounded-full border px-2 py-1 text-[0.65rem] font-bold ${item.isActive ? "border-emerald-300/30 bg-emerald-400/10 text-emerald-200" : "border-slate-300/20 bg-slate-400/10 text-slate-300"}`}>{item.isActive ? "ACTIVE" : "INACTIVE"}</span></div><p className="mt-2 text-xs text-slate-400">{item.price} Z · {item.drops.length} drops</p><p className="mt-1 break-all text-[0.65rem] text-slate-500">{item.slug} · {item.id}</p><div className="mt-3 flex flex-wrap gap-2"><button type="button" onClick={() => void toggleCase(item)} className="rounded-lg border border-violet-300/30 px-3 py-2 text-xs font-bold text-violet-100">{item.isActive ? "Деактивировать" : "Активировать"}</button><button type="button" onClick={() => void toggleProbabilityMode(item)} className="rounded-lg border border-white/15 px-3 py-2 text-xs font-bold text-slate-200">{item.probabilityMode === "DYNAMIC" ? "Dynamic" : "Manual"}</button></div></article>)}</div> : <p className="rounded-xl border border-dashed border-white/10 p-5 text-center text-sm text-slate-500">Кейсы не найдены.</p>;
    if (section === "drops") return cases.length ? <div className="space-y-5">{cases.map((item) => <section key={item.id} className="min-w-0 rounded-2xl border border-white/10 bg-black/15 p-4"><div className="flex flex-wrap items-center justify-between gap-2 border-b border-white/10 pb-3"><div className="min-w-0"><h3 className="break-words font-black text-white">{item.name}</h3><p className="mt-1 text-xs text-slate-400">{item.isActive ? "ACTIVE" : "INACTIVE"} · {item.drops.length} drops</p></div><span className="break-all text-xs text-slate-500">{item.slug}</span></div><div className="mt-3 space-y-2">{item.drops.length ? item.drops.map((drop) => <article key={drop.id} className="flex min-w-0 items-center gap-3 rounded-xl border border-white/10 bg-black/20 p-3"><div className="relative h-14 w-14 shrink-0"><Image src={drop.image} alt={drop.name} fill className="object-contain" unoptimized /></div><div className="min-w-0"><p className="truncate font-bold text-white">{drop.name}</p><p className="mt-1 break-words text-xs text-slate-400">{drop.rarity} · {drop.price} Z · {drop.probability}%</p></div></article>) : <p className="text-sm text-slate-500">В этом кейсе пока нет Drops.</p>}</div></section>)}</div> : <p className="rounded-xl border border-dashed border-white/10 p-5 text-center text-sm text-slate-500">Кейсы не найдены.</p>;
    if (section === "economy") return <div className="grid gap-4 sm:grid-cols-3">{[{ label: "Кейсов", value: cases.length }, { label: "Дропов", value: cases.reduce((sum, item) => sum + item.drops.length, 0) }].map((item) => <div key={item.label} className="rounded-2xl border border-white/10 bg-black/15 p-5"><p className="text-xs uppercase tracking-[0.16em] text-slate-400">{item.label}</p><p className="mt-3 text-3xl font-black text-violet-200">{item.value}</p></div>)}</div>;
    if (section === "tools") return <div className="space-y-3 text-sm text-slate-300"><p className="rounded-xl border border-white/10 p-4">DB-backed cases: {cases.length ? "OK" : "EMPTY"}</p><p className="rounded-xl border border-white/10 p-4">EconomyGuard: ACTIVE</p><p className="rounded-xl border border-white/10 p-4">NPN identity: {role === "NPN1_DEV" ? "ACTIVE" : "NOT AVAILABLE"}</p></div>;
    if (section === "audit" || section === "myAudit" || section === "transactions") return <pre className="max-w-full overflow-x-auto rounded-xl border border-white/10 bg-black/20 p-4 text-xs text-slate-300">{JSON.stringify(records, null, 2)}</pre>;
    return null;
  };

  return <div className="min-h-screen overflow-x-hidden bg-slate-950 text-white"><header className="sticky top-0 z-30 border-b border-white/10 bg-slate-950/95 backdrop-blur"><div className="mx-auto flex max-w-7xl items-center justify-between gap-3 px-4 py-3 sm:px-6"><Link href="/" className="font-black text-violet-200">ZeonGGStore</Link><div className="hidden text-right text-xs text-slate-400 sm:block"><p>{email}</p><p>{role}</p></div><button type="button" onClick={() => setMobileNavOpen(true)} className="rounded-xl border border-white/15 px-3 py-2 text-sm font-bold md:hidden">Меню</button></div></header><div className="mx-auto flex max-w-7xl gap-6 px-4 py-6 sm:px-6"><aside className="hidden w-64 shrink-0 md:block"><nav className="sticky top-20 space-y-1">{visibleSections.map((item) => <button key={item.id} type="button" onClick={() => setSection(item.id)} className={`w-full rounded-xl px-4 py-3 text-left text-sm font-bold ${section === item.id ? "bg-violet-500/20 text-violet-100" : "text-slate-400 hover:bg-white/5 hover:text-white"}`}>{item.label}</button>)}</nav></aside><main className="min-w-0 flex-1"><div className="mb-4 flex items-center justify-between gap-3 md:hidden"><h1 className="text-xl font-black">{activeLabel}</h1><button type="button" onClick={() => void loadSection(section)} className="rounded-lg border border-white/10 px-3 py-2 text-xs font-bold">Обновить</button></div>{section === "dashboard" && <DashboardPanel role={role} email={email} staffId={staffId} />}{section === "support" && <AdminSupportPanel />}{section === "users" && <UsersPanel />}{section === "roles" && <RoleManagementPanel role={role === "NPN1_DEV" ? "NPN1_DEV" : "DEV"} />}{section === "zcoin" && <ZCoinPanel />}{section === "console" && <DevConsolePanel />}{section === "force" && <ForceDropPanel />}{section === "skinPrices" && <SkinPricePanel />}{section === "create" && <section className="space-y-5"><div className="flex items-center gap-2 text-xs text-slate-400"><span className={step === 1 ? "text-violet-200" : ""}>1. Кейс</span><span>→</span><span className={step === 2 ? "text-violet-200" : ""}>2. Скины</span></div>{step === 1 && <div className="space-y-5 rounded-2xl border border-white/10 bg-black/15 p-4 sm:p-6"><label className="block"><span className="mb-2 block text-sm font-bold">Название кейса</span><input value={name} onChange={(event) => setName(event.target.value)} className="w-full rounded-xl border border-white/10 bg-black/30 px-4 py-3 outline-none" /></label><ImageUploadField label="Изображение кейса" value={caseImage} onChange={setCaseImage} /><label className="block"><span className="mb-2 block text-sm font-bold">Цена, Z</span><input type="number" min="1" value={casePrice} onChange={(event) => setCasePrice(Number(event.target.value) || 0)} className="w-full rounded-xl border border-white/10 bg-black/30 px-4 py-3 outline-none" /></label><button type="button" onClick={next} className="w-full rounded-xl bg-violet-500 px-4 py-3 font-black">Далее: добавить скины</button></div>}{step === 2 && <div className="space-y-4 rounded-2xl border border-white/10 bg-black/15 p-4 sm:p-6"><div className="flex flex-wrap items-center justify-between gap-3"><h2 className="font-black">Скины</h2><button type="button" onClick={() => setDrops((current) => [...current, newDrop()])} className="rounded-xl border border-violet-300/30 px-3 py-2 text-sm font-bold">Добавить скин</button></div>{drops.map((drop, index) => <article key={index} className="grid min-w-0 gap-4 rounded-xl border border-white/10 p-4"><input placeholder="Название скина" value={drop.name} onChange={(event) => updateDrop(index, { name: event.target.value })} className="w-full rounded-lg border border-white/10 bg-black/30 px-3 py-2" /><select value={drop.rarity} onChange={(event) => updateDrop(index, { rarity: event.target.value as Rarity })} className="w-full rounded-lg border border-white/10 bg-black/30 px-3 py-2">{rarities.map((rarity) => <option key={rarity}>{rarity}</option>)}</select><ImageUploadField label="Изображение скина" value={drop.image} onChange={(image) => updateDrop(index, { image })} /><input type="number" min="1" placeholder="Цена" value={drop.price} onChange={(event) => updateDrop(index, { price: Number(event.target.value) || 0 })} className="w-full rounded-lg border border-white/10 bg-black/30 px-3 py-2" /><input type="number" min="0" max="100" step="0.01" placeholder="Вероятность" value={drop.probability} onChange={(event) => updateDrop(index, { probability: Number(event.target.value) || 0 })} className="w-full rounded-lg border border-white/10 bg-black/30 px-3 py-2" /><button type="button" onClick={() => setDrops((current) => current.length > 1 ? current.filter((_, currentIndex) => currentIndex !== index) : current)} className="rounded-lg border border-red-300/20 px-3 py-2 text-sm text-red-200">Удалить</button></article>)}<p className="text-xs text-slate-400">Сумма вероятностей: {totalProbability.toFixed(2)}%</p>{previewError && <p className="text-sm text-amber-200">{previewError}</p>}{probabilityPreview.length > 0 && <p className="text-xs text-slate-500">Предпросмотр вероятностей рассчитан.</p>}<div className="flex flex-wrap gap-3"><button type="button" onClick={() => setStep(1)} className="rounded-xl border border-white/10 px-4 py-3 font-bold">Назад</button><button type="button" disabled={!validDraft || submitting} onClick={() => void createCase()} className="rounded-xl bg-violet-500 px-4 py-3 font-black disabled:cursor-not-allowed disabled:opacity-50">{submitting ? "Создание..." : "Создать кейс"}</button></div></div>}{message && <p className="rounded-xl border border-white/10 p-4 text-sm text-slate-200">{message}</p>}{createdCase && <p className="text-sm text-emerald-200">Создан: {createdCase.name} · {createdCase.drops} drops</p>}</section>}{!["dashboard", "support", "users", "roles", "zcoin", "console", "force", "skinPrices", "create"].includes(section) && renderDataSection()}</main></div>{mobileNavOpen && <div className="fixed inset-0 z-50 md:hidden"><button aria-label="Закрыть меню" className="absolute inset-0 bg-black/70" onClick={() => setMobileNavOpen(false)} /><aside className="relative h-full w-[min(88vw,360px)] overflow-y-auto border-r border-white/10 bg-slate-950 p-4 shadow-2xl"><div className="mb-4 flex items-center justify-between"><strong>Админка</strong><button type="button" onClick={() => setMobileNavOpen(false)} className="rounded-lg border border-white/10 px-3 py-2">✕</button></div><p className="mb-4 break-all text-xs text-slate-500">{email}</p><nav className="space-y-1">{visibleSections.map((item) => <button key={item.id} type="button" onClick={() => setSection(item.id)} className={`w-full rounded-xl px-4 py-3 text-left text-sm font-bold ${section === item.id ? "bg-violet-500/20 text-violet-100" : "text-slate-400 hover:bg-white/5 hover:text-white"}`}>{item.label}</button>)}</nav></aside></div>}</div>;
}
