"use client";

import Link from "next/link";
import { useEffect, useRef, useState } from "react";
import RecentDropsStrip from "@/app/components/RecentDropsStrip";
import HelpTip from "@/app/components/HelpTip";
import { getRarityTextClass } from "@/lib/rarity-styles";
import BestDrop from "./components/BestDrop";
import CaseDropList from "./components/CaseDropList";
import CaseRoulette from "./components/CaseRoulette";
import WinnerModal from "./components/WinnerModal";
import { buildRouletteSlots, scoreDrop } from "./lib/roulette";
import type { CaseItem, CatalogCase, CatalogDrop, RouletteAnimationRequest } from "./lib/types";

const RECENT_DROPS_KEY="zeon_recent_drops_v1"; const BEST_DROP_KEY="zeon_best_drop_v1"; const STORE_UPDATE_EVENT="zeon-store-updated";
const readRecentDrops=():CaseItem[]=>{try{const raw=localStorage.getItem(RECENT_DROPS_KEY);const parsed=raw?JSON.parse(raw):[];return Array.isArray(parsed)?parsed.filter(item=>item?.name!=="AK-47 Skin"&&item?.name!=="Knife Skin"&&item?.image!=="/skins/default.png"):[]}catch{return[]}};
const writeRecentDrops=(items:CaseItem[])=>{const fresh=items.filter(item=>!item.timestamp||Date.now()-item.timestamp<604800000).slice(0,50);localStorage.setItem(RECENT_DROPS_KEY,JSON.stringify(fresh));window.dispatchEvent(new Event(STORE_UPDATE_EVENT));};
const readBestDrop=():CaseItem|null=>{try{const raw=localStorage.getItem(BEST_DROP_KEY);const item=raw?JSON.parse(raw):null;return item?.name==="AK-47 Skin"||item?.name==="Knife Skin"||item?.image==="/skins/default.png"?null:item}catch{return null}};
const writeBestDrop=(item:CaseItem|null)=>{if(item)localStorage.setItem(BEST_DROP_KEY,JSON.stringify(item));else localStorage.removeItem(BEST_DROP_KEY);window.dispatchEvent(new Event(STORE_UPDATE_EVENT));};

export default function CasePage(){
 const[catalog,setCatalog]=useState<CatalogCase[]>([]);const[selectedCaseId,setSelectedCaseId]=useState<string|null>(null);const[opening,setOpening]=useState(false);const[winner,setWinner]=useState<CaseItem|null>(null);const[openError,setOpenError]=useState("");const[resultVisible,setResultVisible]=useState(false);const[resultClosing,setResultClosing]=useState(false);const[resultAction,setResultAction]=useState<"inventory"|"sell"|null>(null);const[rouletteSlots,setRouletteSlots]=useState<CaseItem[]>([]);const[winnerIndex,setWinnerIndex]=useState<number|null>(null);const[revealWinner,setRevealWinner]=useState(false);const[bestDrop,setBestDrop]=useState<CaseItem|null>(null);const[expandedChanceCardId,setExpandedChanceCardId]=useState<string|null>(null);const[animating,setAnimating]=useState(false);const[animationRequest,setAnimationRequest]=useState<RouletteAnimationRequest|null>(null);const[resetToken,setResetToken]=useState(0);const[freeOpenAvailable,setFreeOpenAvailable]=useState(false);
 const catalogRef=useRef<CatalogCase[]>([]);const activeCase=catalog.find(item=>item.slug===selectedCaseId||item.id===selectedCaseId)??null;
 const caseSkins:CaseItem[]=activeCase?.drops.map(drop=>({id:drop.id,name:drop.name,rarity:drop.rarity,color:getRarityTextClass(drop.rarity),image:drop.image,price:drop.price,chance:drop.probability,caseId:activeCase.slug,caseImage:activeCase.image}))??[];
 useEffect(()=>{const requested=new URLSearchParams(window.location.search).get("caseId");setSelectedCaseId(requested);void fetch(`/api/cases?version=${Date.now()}`,{cache:"no-store",credentials:"include"}).then(async response=>{if(!response.ok)throw new Error("Не удалось загрузить каталог кейсов");return response.json()}).then((data:{cases?:CatalogCase[]})=>{const next=Array.isArray(data.cases)?data.cases:[];catalogRef.current=next;setCatalog(next);if(!requested)setSelectedCaseId(next[0]?.slug??null)}).catch((error:unknown)=>setOpenError(error instanceof Error?error.message:"Не удалось загрузить каталог кейсов"))},[]);
 useEffect(()=>{const sync=()=>setBestDrop(readBestDrop());sync();window.addEventListener("storage",sync);window.addEventListener(STORE_UPDATE_EVENT,sync);return()=>{window.removeEventListener("storage",sync);window.removeEventListener(STORE_UPDATE_EVENT,sync)}},[]);
 useEffect(()=>{if(!activeCase){setFreeOpenAvailable(false);return;}let cancelled=false;void fetch("/api/cases/open",{method:"POST",headers:{"Content-Type":"application/json"},credentials:"include",body:JSON.stringify({caseId:activeCase.id,preview:true})}).then(r=>r.json().then(data=>({ok:r.ok,data}))).then(({ok,data})=>{if(!cancelled&&ok)setFreeOpenAvailable(Boolean(data?.freeOpenAvailable))}).catch(()=>{if(!cancelled)setFreeOpenAvailable(false)});return()=>{cancelled=true}},[activeCase?.id]);
 useEffect(()=>{if(!activeCase||!caseSkins.length||rouletteSlots.length||opening||animating||resultVisible)return;const preview=buildRouletteSlots(caseSkins);if(!preview.slots.length)return;setWinnerIndex(null);setRevealWinner(false);setRouletteSlots(preview.slots)},[activeCase,caseSkins,rouletteSlots.length,opening,animating,resultVisible]);
 const finishRoll=(requestId:string)=>{if(!animationRequest||animationRequest.id!==requestId)return;const index=animationRequest.winnerIndex;if(index===undefined||index===null||!rouletteSlots[index])return;const rollWinner=rouletteSlots[index];setAnimationRequest(null);setWinnerIndex(index);setRevealWinner(true);setWinner(rollWinner);setResultVisible(true);setOpening(false);setAnimating(false);window.dispatchEvent(new Event("zeon-profile-updated"))};
 const startCaseRoll=async()=>{if(opening||animating)return;let currentCatalog=catalogRef.current;if(!currentCatalog.length){try{const response=await fetch(`/api/cases?version=${Date.now()}`,{cache:"no-store",credentials:"include"});const data=await response.json().catch(()=>null)as{cases?:CatalogCase[];error?:string}|null;if(!response.ok||!Array.isArray(data?.cases))throw new Error(data?.error||"Не удалось загрузить каталог кейсов");currentCatalog=data.cases;catalogRef.current=currentCatalog;setCatalog(currentCatalog)}catch(error){setOpenError(error instanceof Error?error.message:"Не удалось загрузить каталог кейсов");return}}const requested=selectedCaseId??new URLSearchParams(window.location.search).get("caseId");const currentCase=currentCatalog.find(item=>item.slug===requested||item.id===requested)??currentCatalog[0]??null;const currentSkins:CaseItem[]=currentCase?.drops.map(drop=>({id:drop.id,name:drop.name,rarity:drop.rarity,color:getRarityTextClass(drop.rarity),image:drop.image,price:drop.price,chance:drop.probability,caseId:currentCase.slug,caseImage:currentCase.image}))??[];if(!currentCase||!currentSkins.length){setOpenError("Кейс ещё загружается. Попробуй через секунду.");return}if(selectedCaseId!==currentCase.slug)setSelectedCaseId(currentCase.slug);setOpening(true);setOpenError("");setResultVisible(false);setResultClosing(false);setRevealWinner(false);setWinnerIndex(null);setWinner(null);setResultAction(null);setAnimating(false);setAnimationRequest(null);setResetToken(value=>value+1);try{const response=await fetch("/api/cases/open",{method:"POST",headers:{"Content-Type":"application/json"},credentials:"include",body:JSON.stringify({caseId:currentCase.id,idempotencyKey:crypto.randomUUID()})});const data=await response.json().catch(()=>null);if(!response.ok||!data?.drop||!data?.item)throw new Error(data?.error||"Не удалось открыть кейс");setFreeOpenAvailable(false);const serverDrop=data.drop as CatalogDrop;const rollWinner:CaseItem={id:serverDrop.id,name:serverDrop.name,rarity:serverDrop.rarity,color:getRarityTextClass(serverDrop.rarity),image:serverDrop.image,price:serverDrop.price,chance:serverDrop.probability,caseId:currentCase.slug,caseImage:currentCase.image,inventoryItemId:data.item.id};const track=buildRouletteSlots(currentSkins,rollWinner);if(!track.slots.length)throw new Error("Не удалось подготовить рулетку");const request={id:crypto.randomUUID(),winnerIndex:track.winnerSlotIndex};setWinnerIndex(track.winnerSlotIndex);setRouletteSlots(track.slots);setAnimationRequest(request)}catch(error){setOpening(false);setAnimating(false);setAnimationRequest(null);setOpenError(error instanceof Error?error.message:"Не удалось открыть кейс")}};
 const closeResult=()=>{setResultClosing(true);window.setTimeout(()=>{setResultVisible(false);setResultClosing(false);setWinner(null);setRevealWinner(false);setWinnerIndex(null);setResultAction(null);setAnimationRequest(null);setResetToken(value=>value+1);setRouletteSlots([])},250)};
 const handleResultAction=async(action:"inventory"|"sell")=>{if(!winner)return;setResultAction(action);if(action==="sell"){try{const response=await fetch("/api/inventory/sell",{method:"POST",headers:{"Content-Type":"application/json"},credentials:"include",body:JSON.stringify({inventoryItemId:winner.inventoryItemId})});const data=await response.json().catch(()=>null);if(!response.ok)throw new Error(data?.error||"Не удалось продать предмет");window.dispatchEvent(new Event("zeon-profile-updated"))}catch(error){setOpenError(error instanceof Error?error.message:"Не удалось продать предмет");setResultAction(null);return}}closeResult()};
 const handleOpenAgain=()=>{if(resultClosing||opening||animating)return;setResultClosing(false);setResultVisible(false);setWinner(null);setRevealWinner(false);setWinnerIndex(null);setResultAction(null);setAnimationRequest(null);setResetToken(value=>value+1);requestAnimationFrame(()=>void startCaseRoll())};
 useEffect(()=>{if(!winner||!resultVisible)return;const resolved={...winner,timestamp:Date.now()};writeRecentDrops([resolved,...readRecentDrops()]);const current=readBestDrop();writeBestDrop(!current||scoreDrop(winner)>=scoreDrop(current)?winner:current)},[winner,resultVisible]);
 const activeCaseName=activeCase?.name??"Загрузка кейса...";
 return <main className="relative min-h-screen overflow-hidden bg-[#05070b] px-4 py-6 text-white sm:px-6 sm:py-10"><div className="pointer-events-none absolute inset-0 overflow-hidden"><div className="absolute left-1/2 top-16 h-[520px] w-[520px] -translate-x-1/2 rounded-full bg-purple-700/10 blur-[130px]"/></div><div className="relative mx-auto max-w-6xl"><div className="mb-6 flex items-center justify-between gap-4 sm:mb-10"><Link href="/" className="inline-flex items-center gap-2 rounded-full border border-white/10 bg-white/[0.035] px-4 py-2 text-sm font-semibold text-gray-200">← Назад</Link></div><div className="text-center"><div className="flex items-center justify-center gap-2"><p className="text-[11px] font-black uppercase tracking-[0.42em] text-yellow-300">ZEONGGSTORE</p><HelpTip text="Здесь находится выбранный кейс. Внутри указаны доступные предметы, их редкость, стоимость и шанс выпадения." label="Что такое кейс?" /></div><h1 className="mt-3 break-words text-3xl font-black tracking-tight sm:text-5xl">{activeCaseName}</h1><div className="mx-auto mt-3 flex max-w-2xl items-center justify-center gap-2 text-sm leading-6 text-slate-400 sm:text-base"><span>Открой кейс, прокрути рулетку и получи один из предметов текущего кейса.</span><HelpTip text="Открытие списывает стоимость кейса или использует доступное бесплатное открытие. Итоговый предмет определяется сервером до начала анимации." label="Как работает открытие?" /></div></div><div className="mt-8 sm:mt-10"><RecentDropsStrip title="Последние дропы"/></div><BestDrop bestDrop={bestDrop}/><section className="mt-8 sm:mt-10"><div className="rounded-[28px] border border-purple-300/10 bg-[radial-gradient(circle_at_center,rgba(109,40,217,0.16),rgba(7,9,14,0.96)_62%)] p-5 shadow-[0_30px_100px_rgba(0,0,0,0.45)] sm:p-8"><div className="flex flex-wrap items-center justify-between gap-3"><div><div className="flex items-center gap-2"><p className="text-[10px] font-black uppercase tracking-[0.28em] text-slate-500">Кейс</p><HelpTip text="Изображение кейса здесь больше не используется — основной акцент страницы сделан на рулетке, шансах и самом открытии." label="Зачем убрана картинка кейса?" /></div><p className="mt-2 text-2xl font-black text-white sm:text-3xl">{activeCaseName}</p></div><div className="rounded-2xl border border-white/10 bg-white/[0.035] px-4 py-3 text-right"><p className="text-[10px] font-black uppercase tracking-[0.22em] text-slate-500">Предметов</p><p className="mt-1 text-xl font-black text-white">{caseSkins.length}</p></div></div></div></section><section className="mt-8 sm:mt-10"><div className="mb-3 flex items-center justify-between gap-4 px-1"><div className="flex items-center gap-2"><p className="text-[10px] font-black uppercase tracking-[0.28em] text-slate-500">Рулетка</p><HelpTip text="Рулетка показывает визуальную анимацию открытия. Указатель находится по центру, а результат уже выбран сервером до начала анимации." label="Как работает рулетка?" /></div><span className="hidden text-xs font-semibold text-slate-500 sm:block">Указатель всегда стоит по центру</span></div><CaseRoulette slots={rouletteSlots} winnerIndex={winnerIndex} revealWinner={revealWinner} request={animationRequest} resetToken={resetToken} onAnimatingChange={setAnimating} onFinished={finishRoll}/></section>{!resultVisible&&<section className="mt-6 text-center sm:mt-8">{openError&&<p className="mx-auto mt-4 max-w-xl text-sm font-semibold text-red-300">{openError}</p>}{freeOpenAvailable&&<div className="mt-4 flex items-center justify-center gap-2 text-sm font-bold text-emerald-300"><span>Промокод активен: этот кейс можно открыть бесплатно.</span><HelpTip text="Бесплатное открытие доступно по активному промокоду или другому разрешённому бонусу. При открытии стоимость не списывается." label="Что значит бесплатно?" /></div>}<div className="mt-5 flex items-center justify-center gap-2"><button type="button" onClick={()=>void startCaseRoll()} disabled={opening||animating} className="group relative min-w-[250px] overflow-hidden rounded-2xl bg-gradient-to-b from-yellow-200 to-amber-400 px-10 py-5 text-lg font-black text-[#17120a] shadow-[0_0_36px_rgba(250,204,21,0.22)] disabled:cursor-wait disabled:opacity-70"><span className="relative">{opening||animating?"Открываем...":freeOpenAvailable?"Открыть бесплатно":"Открыть кейс"}</span></button><HelpTip text="Нажми кнопку, чтобы начать открытие. Во время анимации повторное нажатие заблокировано, чтобы не создать двойное открытие." label="Что делает кнопка открытия?" /></div></section>}{winner&&resultVisible&&<WinnerModal winner={winner} resultClosing={resultClosing} resultAction={resultAction} onAction={handleResultAction} onOpenAgain={handleOpenAgain}/>}<section className="mt-8"><CaseDropList drops={caseSkins} expandedChanceCardId={expandedChanceCardId} onToggleChance={id=>setExpandedChanceCardId(current=>current===id?null:id)}/></section></div></main>;
}
