"use client";
import { useEffect, useState } from "react";
import Link from "next/link";
import Header from "@/app/components/Header";
import RecentDropsStrip from "@/app/components/RecentDropsStrip";

type CaseItem = { id: string; name: string; image: string; price: number };
type WheelItem = { type: string; label: string; icon: string; weight: number };
type Result = { rewardType: string; rewardValue: number | null; caseId: string | null; label: string; code?: string | null; sectorIndex?: number };
const fallbackWheel: WheelItem[] = [
  { type: "DEPOSIT", label: "Депозит", icon: "%", weight: 18 }, { type: "CASE", label: "Бесплатный кейс", icon: "▣", weight: 18 }, { type: "BALANCE", label: "Бесплатный баланс", icon: "Z", weight: 16 }, { type: "ZCOIN", label: "Z-Coin", icon: "Z", weight: 15 }, { type: "ZCOIN", label: "Большой Z-Coin", icon: "Z", weight: 8 }, { type: "DEPOSIT", label: "Депозит", icon: "%", weight: 10 }, { type: "CASE", label: "Бесплатный кейс", icon: "▣", weight: 8 }, { type: "BALANCE", label: "Бесплатный баланс", icon: "Z", weight: 7 },
];
function FortuneIcon({ item }: { item: WheelItem }) { return <div className={`flex h-14 w-14 items-center justify-center rounded-2xl border text-xl font-black shadow-[0_0_24px_rgba(168,85,247,.18)] ${item.type === "DEPOSIT" ? "border-fuchsia-300/40 bg-fuchsia-500/15 text-fuchsia-200" : item.type === "CASE" ? "border-orange-300/40 bg-orange-500/15 text-orange-200" : "border-violet-300/40 bg-violet-500/15 text-violet-200"}`}>{item.icon}</div>; }
export default function BonusesPage() {
  const [wheel, setWheel] = useState<WheelItem[]>(fallbackWheel), [cases, setCases] = useState<CaseItem[]>([]), [spinning, setSpinning] = useState(false), [rotation, setRotation] = useState(0), [result, setResult] = useState<Result | null>(null), [error, setError] = useState(""), [info, setInfo] = useState(false), [promo, setPromo] = useState(""), [notice, setNotice] = useState("");
  useEffect(() => { void fetch("/api/bonuses/fortune", { cache: "no-store" }).then(async r => { const d = await r.json(); if (r.ok) { setWheel(d.wheel ?? fallbackWheel); setCases(d.cases ?? []); } }).catch(() => undefined); }, []);
  const selectedCase = result?.caseId ? cases.find(c => c.id === result.caseId) : null;
  const spin = async () => { if (spinning) return; setSpinning(true); setResult(null); setError(""); try { const response = await fetch("/api/bonuses/fortune", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ idempotencyKey: crypto.randomUUID() }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || "Не удалось запустить колесо."); const sector = Number.isInteger(data.sectorIndex) ? data.sectorIndex : 0; setRotation(current => current + 1440 - sector * 45); window.setTimeout(() => { setResult(data); setSpinning(false); window.dispatchEvent(new Event("zeon-profile-updated")); }, 2800); } catch (e) { setError(e instanceof Error ? e.message : "Ошибка колеса."); setSpinning(false); } };
  return <main className="min-h-screen overflow-x-hidden bg-[#05070d] text-white"><Header/><section className="px-4 pb-12 pt-6 sm:px-6 lg:px-8"><div className="mx-auto max-w-[1440px] overflow-hidden rounded-[30px] border border-white/10 bg-[#070b11]/90"><div className="border-b border-white/10 px-4 py-4 sm:px-6"><RecentDropsStrip title="Последние дропы"/></div><div className="px-4 py-9 sm:px-6 lg:px-10 lg:py-14"><div className="mx-auto max-w-6xl"><div className="mb-9 text-center"><p className="text-[0.72rem] font-semibold uppercase tracking-[0.32em] text-violet-300/90">ZEONGGSTORE • БОНУСЫ</p><h1 className="mt-3 text-4xl font-black uppercase tracking-[-0.06em] sm:text-5xl">Колесо фортуны</h1><p className="mx-auto mt-4 max-w-2xl text-sm leading-6 text-slate-400">8 трапециевидных ячеек. Сервер выбирает результат, а анимация всегда останавливается на выпавшей ячейке.</p></div><div className="grid gap-7 lg:grid-cols-[1.1fr_.9fr]"><div className="rounded-[30px] border border-violet-400/20 bg-[#0a0f18]/90 p-4 shadow-[0_0_70px_rgba(124,58,237,.10)] sm:p-7"><div className="relative mx-auto max-w-[660px] overflow-hidden py-5"><div className="pointer-events-none absolute left-1/2 top-0 z-20 h-8 w-10 -translate-x-1/2"><div className="mx-auto h-0 w-0 border-x-[12px] border-b-[22px] border-x-transparent border-b-violet-200 drop-shadow-[0_0_10px_rgba(168,85,247,.8)]"/></div><div className="relative mx-auto aspect-square max-w-[590px] rounded-full border-[10px] border-[#181326] bg-[radial-gradient(circle,#171126_0%,#0b0c15_68%,#05070d_100%)] p-3 shadow-[0_0_70px_rgba(124,58,237,.22)]" style={{ transform: `rotate(${rotation}deg)`, transition: spinning ? "transform 2.8s cubic-bezier(.12,.76,.16,1)" : "none" }}>{wheel.map((item,i) => <div key={`${item.type}-${i}`} className="absolute left-1/2 top-1/2 h-[47%] w-[26%] -translate-x-1/2 -translate-y-1/2 origin-center" style={{ transform: `rotate(${i*45}deg) translateY(-48%)` }}><div className="relative flex h-full w-full items-center justify-center" style={{ clipPath: "polygon(18% 0,82% 0,100% 100%,0 100%)" }}><div className="absolute inset-[1px] border border-white/10 bg-[#111827]" style={{ clipPath: "polygon(18% 0,82% 0,100% 100%,0 100%)" }}/><div className="relative z-10 flex flex-col items-center gap-2 text-center"><FortuneIcon item={item}/><span className="max-w-[90px] text-[10px] font-black uppercase leading-3 text-slate-200">{item.label}</span></div></div></div>)}<div className="absolute left-1/2 top-1/2 flex h-24 w-24 -translate-x-1/2 -translate-y-1/2 items-center justify-center rounded-full border-8 border-[#090a11] bg-gradient-to-br from-violet-500 to-fuchsia-600 text-xs font-black uppercase tracking-[.12em] shadow-[0_0_35px_rgba(168,85,247,.6)]">ZEON</div></div></div><div className="mt-5 flex flex-col items-center gap-3"><button type="button" disabled={spinning} onClick={() => void spin()} className="w-full max-w-sm rounded-2xl bg-gradient-to-r from-violet-600 to-fuchsia-600 px-7 py-4 text-sm font-black uppercase tracking-[.12em] shadow-[0_0_32px_rgba(168,85,247,.35)] transition hover:brightness-110 disabled:opacity-50">{spinning ? "Колесо крутится..." : "Крутить колесо"}</button><button type="button" onClick={() => setInfo(v=>!v)} className="text-xs font-bold text-violet-300">? Как работает колесо</button>{info&&<div className="max-w-lg rounded-2xl border border-white/10 bg-white/[.03] p-4 text-xs leading-5 text-slate-300">Депозит: 5–30%. Бесплатный баланс: 5–50 Z-Coin. Бесплатный кейс берётся из реальных PNG кейсов сайта; дешёвые кейсы имеют больший шанс. Если уже есть депозитный бонус, меньший новый бонус не заменяет больший.</div>}</div></div><div className="space-y-5"><div className="rounded-[26px] border border-white/10 bg-[#0a0f18]/90 p-5 sm:p-6"><h2 className="text-xl font-black">8 панелей</h2><div className="mt-4 grid grid-cols-2 gap-3">{wheel.map((item,i)=><div key={i} className="flex min-h-[92px] flex-col items-center justify-center rounded-2xl border border-white/10 bg-white/[.025] p-3 text-center"><FortuneIcon item={item}/><span className="mt-2 text-[10px] font-black uppercase text-slate-300">{item.label}</span></div>)}</div></div>{result&&<div className="rounded-[26px] border border-violet-300/25 bg-violet-500/10 p-5 sm:p-6"><p className="text-[10px] font-black uppercase tracking-[.18em] text-violet-300">Твоя награда</p><h2 className="mt-2 text-2xl font-black">{result.label}</h2>{selectedCase&&<div className="mt-4 flex items-center gap-4 rounded-2xl border border-white/10 bg-black/20 p-3"><img src={selectedCase.image} alt={selectedCase.name} className="h-20 w-20 object-contain"/><div><p className="font-black">{selectedCase.name}</p><p className="text-xs text-slate-400">Цена: {selectedCase.price} Z-Coin</p></div></div>}{result.rewardType==="DEPOSIT"&&result.code&&<><p className="mt-3 text-xs text-slate-400">Промокод хранится как бонус и готов к пополнению.</p><div className="mt-3 rounded-xl border border-white/10 bg-black/20 px-4 py-3 font-mono text-sm">{result.code}</div><Link href={`/deposit?bonus=${encodeURIComponent(result.code)}`} className="mt-3 block rounded-xl bg-violet-600 px-4 py-3 text-center text-xs font-black uppercase">Применить и пополнить</Link></>}</div>}{error&&<div className="rounded-2xl border border-red-400/20 bg-red-500/10 p-4 text-sm text-red-200">{error}</div>}</div></div></div></div></div></section></main>;
}
