"use client";

import { useState, type FormEvent } from "react";

type Ticket = { id: string; subject: string; description: string; status: string; createdAt: string };
type Message = { id: string; body: string; createdAt: string; author: { name: string | null; email: string; role: string } };
function date(value: string) { return new Intl.DateTimeFormat("ru-RU", { dateStyle: "short", timeStyle: "short" }).format(new Date(value)); }
function HelpIcon(){return <svg viewBox="0 0 24 24" className="h-4 w-4" fill="none" stroke="currentColor" strokeWidth="2"><circle cx="12" cy="12" r="9"/><path d="M9.5 9a2.8 2.8 0 1 1 4.8 2c-.9.9-2.3 1.5-2.3 3.1" strokeLinecap="round"/><path d="M12 17h.01" strokeLinecap="round" strokeWidth="3"/></svg>}
export default function SupportTicketPanel({ tickets, onRefresh }: { tickets: Ticket[]; onRefresh: () => Promise<void> }) {
  const [subject,setSubject]=useState(""); const [description,setDescription]=useState(""); const [selected,setSelected]=useState<{ticket:Ticket;messages:Message[]}|null>(null); const [reply,setReply]=useState(""); const [error,setError]=useState(""); const [success,setSuccess]=useState(""); const [busy,setBusy]=useState(false); const [info,setInfo]=useState(false);
  const createTicket=async(event:FormEvent)=>{event.preventDefault();setBusy(true);setError("");setSuccess("");try{const response=await fetch("/api/support",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({subject,description})});const data=await response.json().catch(()=>null);if(!response.ok)setError(data?.error||"Не удалось создать обращение");else{setSubject("");setDescription("");setSuccess("Обращение создано. Теперь оно находится в списке ниже.");await onRefresh();}}catch{setError("Не удалось создать обращение. Попробуй ещё раз.");}finally{setBusy(false);}};
  const openTicket=async(ticketId:string)=>{setError("");try{const response=await fetch(`/api/support/${ticketId}/messages`,{cache:"no-store"});const data=await response.json().catch(()=>null);if(response.ok)setSelected({ticket:data.ticket,messages:data.ticket.messages});else setError(data?.error||"Не удалось открыть обращение.");}catch{setError("Не удалось открыть обращение.");}};
  const sendReply=async(event:FormEvent)=>{event.preventDefault();if(!selected||!reply.trim())return;setBusy(true);setError("");try{const response=await fetch(`/api/support/${selected.ticket.id}/messages`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({body:reply})});if(response.ok){setReply("");await openTicket(selected.ticket.id);await onRefresh();}else{const data=await response.json().catch(()=>null);setError(data?.error||"Не удалось отправить сообщение");}}catch{setError("Не удалось отправить сообщение.");}finally{setBusy(false);}};
  return <div className="grid gap-5 xl:grid-cols-[0.8fr_1.2fr]"><div><form onSubmit={createTicket} className="space-y-3 rounded-[18px] border border-white/10 bg-[#0b1017] p-5"><div className="flex items-center gap-2"><p className="text-sm font-black text-white">Новое обращение</p><div className="relative"><button type="button" onClick={()=>setInfo(v=>!v)} aria-label="Как работает поддержка" className="flex h-7 w-7 items-center justify-center rounded-full border border-violet-400/40 text-violet-200"><HelpIcon/></button>{info&&<div className="absolute bottom-9 left-0 z-20 w-72 rounded-xl border border-violet-300/20 bg-[#111827] p-3 text-xs leading-5 text-slate-300 shadow-xl"><b className="text-white">Как это работает:</b><br/>1. Выбери тему.<br/>2. Подробно опиши проблему.<br/>3. Создай обращение.<br/>4. Нажми на него в списке и жди ответа или продолжай переписку.</div>}</div></div><div><label className="mb-1 block text-xs font-bold text-slate-300">О чём проблема?</label><select value={subject} onChange={event=>setSubject(event.target.value)} required className="w-full rounded-xl border border-white/10 bg-[#080d15] px-3 py-2.5 text-sm text-white"><option value="">Выберите тему</option><option>Предмет не появился</option><option>Проблема с открытием кейса</option><option>Проблема с продажей</option><option>Проблема с балансом</option><option>Проблема с транзакцией</option><option>Другое</option></select></div><div><label className="mb-1 block text-xs font-bold text-slate-300">Что произошло?</label><p className="mb-2 text-xs text-slate-500">Напиши, что ты делал и какая проблема появилась. Чем подробнее, тем легче помочь.</p><textarea value={description} onChange={event=>setDescription(event.target.value)} required rows={5} placeholder="Например: открыл кейс, предмет выпал, но не появился в инвентаре..." className="w-full resize-none rounded-xl border border-white/10 bg-[#080d15] px-3 py-2.5 text-sm text-white placeholder:text-slate-500"/></div><button type="submit" disabled={busy} className="rounded-xl border border-violet-400/40 bg-violet-500/10 px-4 py-2.5 text-sm font-semibold text-violet-100 disabled:opacity-50">{busy?"Отправка...":"Создать обращение"}</button></form>{success&&<p className="mt-3 rounded-xl border border-emerald-400/30 bg-emerald-500/10 p-3 text-sm text-emerald-100">{success}</p>}{error&&<p className="mt-3 rounded-xl border border-red-400/30 bg-red-500/10 p-3 text-sm text-red-200">{error}</p>}<div className="mt-5 space-y-3">{tickets.length?tickets.map(ticket=><button type="button" key={ticket.id} onClick={()=>void openTicket(ticket.id)} className={`block w-full rounded-xl border p-4 text-left ${selected?.ticket.id===ticket.id?"border-violet-300/50 bg-violet-500/10":"border-white/10 bg-[#0b1017] hover:border-violet-300/40"}`}><div className="flex justify-between gap-3"><span className="font-semibold text-white">{ticket.subject}</span><span className="text-xs text-violet-200">{ticket.status}</span></div><p className="mt-2 text-sm text-slate-400">{date(ticket.createdAt)}</p></button>):<div className="rounded-xl border border-dashed border-white/10 p-5 text-center text-sm text-slate-400">Обращений пока нет</div>}</div></div><section className="rounded-[18px] border border-white/10 bg-[#0b1017] p-5">{selected?<><div className="border-b border-white/10 pb-4"><div className="flex items-start justify-between gap-3"><div><h2 className="font-black text-white">{selected.ticket.subject}</h2><p className="mt-2 text-sm text-slate-400">{selected.ticket.description}</p></div><span className="shrink-0 rounded-lg border border-violet-400/20 px-2 py-1 text-xs text-violet-200">{selected.ticket.status}</span></div></div><div className="max-h-80 space-y-3 overflow-y-auto py-4">{selected.messages.length?selected.messages.map(message=><div key={message.id} className="rounded-xl border border-white/10 bg-black/15 p-3"><p className="text-xs font-bold text-violet-200">{message.author.name||message.author.email}</p><p className="mt-1 whitespace-pre-wrap text-sm text-slate-200">{message.body}</p><p className="mt-2 text-[0.68rem] text-slate-500">{date(message.createdAt)}</p></div>):<p className="text-sm text-slate-500">Сообщений пока нет.</p>}</div><form onSubmit={sendReply} className="flex gap-2 border-t border-white/10 pt-4"><input value={reply} onChange={event=>setReply(event.target.value)} placeholder="Напиши сообщение в поддержку" className="min-w-0 flex-1 rounded-xl border border-white/10 bg-black/20 px-3 py-2.5 text-sm text-white"/><button type="submit" disabled={busy||!reply.trim()} className="rounded-xl bg-violet-500 px-4 py-2.5 text-sm font-bold disabled:opacity-40">Отправить</button></form></>:<div className="flex min-h-64 items-center justify-center text-center text-sm text-slate-500">Выберите обращение слева, чтобы открыть переписку.</div>}</section></div>;
}
